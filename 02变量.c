#include <stdio.h>

int a = 100;

int main()
{
    int a = 10;
    printf("a=%d\n", a);
    
    return 0;
}
//此时结果输出为10还是100？
//结果是10，因为全局变量和局部变量符合局部变量优先原则

//全局变量的作用域是整个工程，声明一下就可以跨文件使用
//局部变量的使用范围是他最近的那个大括号组所括起来的范围

//全局变量的生命周期是主函数范围
//局部变量的生命周期是大括号